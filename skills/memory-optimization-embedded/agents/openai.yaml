interface:
  display_name: "Optimize firmware memory"
  short_description: "Shrink a feature's RAM and Flash"
  default_prompt: "Use $memory-optimization-embedded to profile this feature and safely reduce its RAM and Flash use."
policy:
  allow_implicit_invocation: false
