interface:
  display_name: "Simplify firmware"
  short_description: "Simplify firmware without changing behavior"
  default_prompt: "Use $code-simplification-embedded to simplify this feature."
policy:
  allow_implicit_invocation: false
